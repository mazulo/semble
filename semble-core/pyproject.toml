[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "semble-core"
version = "0.1.0"
description = "Native tokenization extensions for semble"
requires-python = ">=3.10"

[tool.maturin]
module-name = "semble_core"
